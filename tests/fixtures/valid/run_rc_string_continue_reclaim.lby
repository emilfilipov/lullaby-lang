fn main -> i64
    let total i64 = 0
    for i from 0 to 300000
        let s string = to_string(i) + "!!!!!!!!!!"
        total = total + len(s)
        if i % 2 == 0
            continue
        total = total + 1
    total
