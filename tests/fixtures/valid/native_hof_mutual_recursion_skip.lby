fn ping f fn(i64) -> i64 v i64 -> i64
    if v <= 0
        return f(0)
    pong(f, v - 1)

fn pong f fn(i64) -> i64 v i64 -> i64
    ping(f, v - 1)

fn main -> i64
    let n i64 = 9
    let c fn(i64) -> i64 = fn x i64 -> x + n
    ping(c, 4)
