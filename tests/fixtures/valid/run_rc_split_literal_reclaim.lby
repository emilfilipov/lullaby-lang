fn main -> i64
    let total i64 = 0
    for i from 0 to 100000
        let parts array<string> = split("aaa,bbb,ccc,ddd", ",")
        total = total + len(parts)
        for j from 0 to len(parts) - 1
            total = total + len(parts[j])
    total
